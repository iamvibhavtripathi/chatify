{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
      "client-install":"cd frontend && npm install",
    "client-build":"cd frontend && npm run build"

  },
        "engineStrict": true,
  "engines": {
    "node": "v14.20.1"
  },

  "author": "VIBHAV TRIPATHI",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.4.0",
    "cores": "^0.8.5",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^7.1.1",
    "nodemon": "^2.0.22",
    "socket.io": "^4.6.1"
  }
}
